diff --git a/headless/lib/browser/headless_screen.cc b/headless/lib/browser/headless_screen.cc
index 9f577bee6dd6376bb1988dec7061b6e9c2fcc974..9107536c98554e044d5679df940f152f4ff6af03 100644
--- a/headless/lib/browser/headless_screen.cc
+++ b/headless/lib/browser/headless_screen.cc
@@ -31,7 +31,13 @@ HeadlessScreen* HeadlessScreen::Create(const gfx::Size& window_size,
 HeadlessScreen::~HeadlessScreen() = default;

 gfx::Point HeadlessScreen::GetCursorScreenPoint() {
-  return gfx::Point();
+  // Return a realistic cursor position instead of always (0,0)
+  // Use a position in the middle of the primary display
+  if (!display_list().displays().empty()) {
+    const auto& bounds = display_list().displays()[0].bounds();
+    return gfx::Point(bounds.width() / 2, bounds.height() / 2);
+  }
+  return gfx::Point(960, 540);  // Fallback to center of 1920x1080
 }

 bool HeadlessScreen::IsWindowUnderCursor(gfx::NativeWindow window) {
